mass = float(input("Enter the mass (in Kg): "))             #Asks user about both mass and velocity values and stores them as float values.
velocity = float(input("Enter the velocity (in m/s): "))    
KE = (0.5*mass*velocity**2)                                 #Calculates kinetic energy and stores it in "KE" 
print(KE)                                                   #Prints the previusly calculated KE.